// Some points here... we have a different package name for the non-game code,
// and we need to make sure that we handle that.  We also have a duplicate
// funciotn name between the game code and the non-game code. That should also
// work correctly.

package lib

func Build() {]}
